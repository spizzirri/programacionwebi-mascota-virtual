services:
  mongodb:
    image: mongo:8
    container_name: tamagotchi-mongodb
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db
    restart: always

volumes:
  mongodb_data:
